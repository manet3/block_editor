<UserControl x:Class="SimpleBlockEditor.Connector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimpleBlockEditor"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" 
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <!--<Path Data="M 5 0 L 10 10 L 0 10 L 5 0" Fill="Red" Height="10" Width="10">
            <Path.RenderTransform>
                <RotateTransform CenterX="5" CenterY="5" Angle="140"/>
            </Path.RenderTransform>
        </Path>-->
        <Line X1="{Binding Block1.X}" Y1="{Binding Block1.Y}" 
              X2="{Binding Block2.X}" Y2="{Binding Block2.Y}"
              StrokeThickness="3" Stroke="Black"></Line>
    </Grid>
</UserControl>
